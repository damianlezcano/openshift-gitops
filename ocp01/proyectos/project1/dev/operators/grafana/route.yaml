kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: console
  labels:
    app: grafana
spec:
  to:
    kind: Service
    name: grafana-service
    weight: 100
  port:
    targetPort: grafana
  wildcardPolicy: None